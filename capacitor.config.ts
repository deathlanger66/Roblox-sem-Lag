import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iago.bloxstrapmobilehelper',
  appName: 'Bloxstrap Mobile Helper',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
